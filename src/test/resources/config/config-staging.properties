# Staging environment overlay. Only keys that differ from config.properties belong here.
# Run with: mvn test -Denv=staging
apiBaseUri=https://staging.jsonplaceholder.typicode.com

# Staging is typically slower than QA, so give synchronization more headroom.
explicit.wait.seconds=15
